---
// CardTitle.astro
export interface CardTitleProps {
  className?: string;
}

const { className } = Astro.props;
---

<div class={`font-semibold leading-none tracking-tight ${className ?? ""}`}>
  <slot />
</div>
